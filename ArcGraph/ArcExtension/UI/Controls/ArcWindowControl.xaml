<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="8">
            <TextBlock Text="{Binding StatusMessage}"
               Foreground="Red"
               TextWrapping="Wrap"
               Margin="0,0,0,8" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Refresh"
                        Width="80"
                        Margin="0,0,8,0"
                        Command="{Binding RefreshCommand}" />

                <Button Content="Analyze"
                        Width="80"
                        Command="{Binding AnalyzeCommand}" />
            </StackPanel>

            <WrapPanel VerticalAlignment="Center" Margin="0,8,0,0">
                <StackPanel Orientation="Horizontal" Margin="4" VerticalAlignment="Center">
                    <Rectangle Width="16" Height="16" Fill="#1f77b4" Stroke="Black" StrokeThickness="0.5" Margin="0,0,4,0"/>
                    <TextBlock VerticalAlignment="Center" Text="UI" Margin="0,0,8,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="4" VerticalAlignment="Center">
                    <Rectangle Width="16" Height="16" Fill="#2ca02c" Stroke="Black" StrokeThickness="0.5" Margin="0,0,4,0"/>
                    <TextBlock VerticalAlignment="Center" Text="Application" Margin="0,0,8,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="4" VerticalAlignment="Center">
                    <Rectangle Width="16" Height="16" Fill="#ff7f0e" Stroke="Black" StrokeThickness="0.5" Margin="0,0,4,0"/>
                    <TextBlock VerticalAlignment="Center" Text="Domain" Margin="0,0,8,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="4" VerticalAlignment="Center">
                    <Rectangle Width="16" Height="16" Fill="#9467bd" Stroke="Black" StrokeThickness="0.5" Margin="0,0,4,0"/>
                    <TextBlock VerticalAlignment="Center" Text="Infrastructure" Margin="0,0,8,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="4" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Fill="#d9534f" Stroke="Black" StrokeThickness="0.5" Margin="0,0,4,0"/>
                    <TextBlock VerticalAlignment="Center" Text="Vulnerable package" Margin="0,0,8,0"/>
                </StackPanel>
            </WrapPanel>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,8"
                   FontWeight="Bold"
                   Text="{Binding StatusMessage}" />

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Files}"
                 Height="120">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               TextTrimming="CharacterEllipsis" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,8,0,8">
            <TextBlock FontWeight="Bold" Text="Vulnerable packages in solution:" />
            <ListBox ItemsSource="{Binding VulnerablePackages}" Height="80">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <wv2:WebView2
        Grid.Row="4"
        Name="webView2"
        Source="{Binding WebViewUri}"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
            <wv2:WebView2.CreationProperties>
                <wv2:CoreWebView2CreationProperties UserDataFolder="C:\temp\myFolder"/>
            </wv2:WebView2.CreationProperties>
        </wv2:WebView2>

    </Grid>
</DataTemplate>